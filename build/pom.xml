<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>jetbrains.teamcity</groupId>
    <artifactId>cmake-runner</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>
  <artifactId>build</artifactId>
  <packaging>pom</packaging>
  <properties>
    <maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
    <teamcity.plugin.version>${teamcityPluginVersion}</teamcity.plugin.version>
    <!--<teamcity.plugin.version>snapshot-${maven.build.timestamp}</teamcity.plugin.version>-->
  </properties>
  <dependencies>
    <dependency>
      <groupId>jetbrains.teamcity</groupId>
      <artifactId>cmake-runner-agent</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>jetbrains.teamcity</groupId>
      <artifactId>cmake-runner-common</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>
    <dependency>
      <groupId>jetbrains.teamcity</groupId>
      <artifactId>cmake-runner-server</artifactId>
      <version>1.0-SNAPSHOT</version>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <plugin>
        <groupId>com.google.code.maven-replacer-plugin</groupId>
        <artifactId>replacer</artifactId>
        <version>1.5.2</version>
        <executions>
          <execution>
            <phase>process-sources</phase>
            <goals>
              <goal>replace</goal>
            </goals>
          </execution>
        </executions>
        <configuration>
          <file>${basedir}/../cmake-runner-server/teamcity-server-plugin.xml</file>
          <outputFile>${basedir}/target/teamcity-plugin.xml</outputFile>
          <replacements>
            <replacement>
              <!--suppress MavenModelInspection -->
              <token>@Plugin_Version@</token>
              <value>${teamcity.plugin.version}</value>
            </replacement>
            <replacement>
              <!--suppress MavenModelInspection -->
              <token>@Plugin_Vendor@</token>
              <value>JetBrains, s.r.o.</value>
            </replacement>
            <replacement>
              <!--suppress MavenModelInspection -->
              <token>@Plugin_Vendor_Url@</token>
              <value>http://www.jetbrains.com</value>
            </replacement>
          </replacements>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-assembly-plugin</artifactId>
        <version>2.4</version>
        <executions>
          <execution>
            <id>make-agent-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <finalName>cmake-runner-agent</finalName>
              <appendAssemblyId>false</appendAssemblyId>
              <descriptors>
                <descriptor>plugin-agent-assembly.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
          <execution>
            <id>make-assembly</id>
            <phase>package</phase>
            <goals>
              <goal>single</goal>
            </goals>
            <configuration>
              <finalName>cmake-runner</finalName>
              <outputDirectory>${project.parent.build.directory}</outputDirectory>
              <appendAssemblyId>false</appendAssemblyId>
              <descriptors>
                <descriptor>plugin-assembly.xml</descriptor>
              </descriptors>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
